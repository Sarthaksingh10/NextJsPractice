//This is the example of the route group we have to write the main folder inside the () brackets so that 
//The structure of the folder is maintain and it will not be affected by any other routes

import React from 'react'

export default function page() {
  return <h1>Forgot Password</h1>
}
